# Test dependencies — install alongside requirements.txt
# pip install -r requirements-test.txt

pytest>=7.4
pytest-flask>=1.3
pytest-cov>=4.1
freezegun>=1.4       # freeze time for date-sensitive tests
bcrypt>=4.0          # already in main deps but explicit here
