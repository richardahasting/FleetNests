DATABASE_URL=postgresql://bentley_user:changeme@localhost/bentley_boat
SECRET_KEY=change-me-in-production
APP_PREFIX=/boat
APPROVAL_REQUIRED=false

# Email notifications (uses local Postfix — no SMTP auth needed on same server)
EMAIL_ENABLED=false
EMAIL_FROM=noreply@yourdomain.org
APP_URL=https://yourdomain.org/boat
SMTP_HOST=localhost
SMTP_PORT=25
